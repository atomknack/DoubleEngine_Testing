using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;
using DoubleEngine;
using Newtonsoft.Json;

public partial class TestGenerators
{
    //public static Vec3I RandVec3I(int min = -10000, int max = 10000) => 
    //    new Vec3I(rand.Next(min, max), rand.Next(min, max), rand.Next(min, max));
}